function PageStub({ titulo }) {
  return (
    <section className="q-card q-panel q-empty">
      <h3>{titulo}</h3>
      <p>Esta seção está sendo portada para o novo layout.</p>
    </section>
  );
}

export default PageStub;
